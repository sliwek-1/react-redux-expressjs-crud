export const routes = {
    server: "http://localhost:3000",
    route: {
        api: {
            users: {
                get: "/api/allUsers",
                delete: "/api/deleteUser",
                post: "/api/createUser"
            }
        }
    }
}